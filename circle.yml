machine:
  java:
    version: openjdk8
#dependencies
dependencies:
  override:
    - mvn clean package mule:deploy -Denvironment=Development -DapplicationName=testmaven-cbi-dev integration-test -s settings.xml
#test
test:
  override:
    - mvn -Denvironment=Test -DapplicationName=testmaven-cbi-test mule:deploy
    - echo 'Skip Tests'

  