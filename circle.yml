machine:
  java:
    version: openjdk8
#dependencies
dependencies:
  override:
    - mvn clean package mule:deploy -Denvironment=Development integration-test -s settings.xml
#test
test:
  override:
    - mvn mule:deploy -Denvironment=Test
    - echo 'Skip Tests'

  