machine:
  java:
    version: openjdk8
#dependencies
dependencies:
  override:
    - mvn clean package mule:deploy -DapplicationName="testmaven-cbi-dev" -Denvironment="Development" integration-test -s settings.xml
#test
test:
  override:
    - mvn -DapplicationName="testmaven-cbi-test" -Denvironment="Test" mule:deploy
    - echo 'Skip Tests'

  