machine:
  java:
    version: openjdk8
#dependencies
dependencies:
  override:
    - mvn clean package mule:deploy integration-test -s settings.xml
#test
test:
  override:
    - mvn mule:deploy -Denvironment=Test,applicationName=testmaven-cbi-test
     - echo 'Skip Tests'

  