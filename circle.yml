machine:
  java:
    version: openjdk8
#dependencies
dependencies:
  override:
    - mvn clean package mule:deploy@deploy-devch integration-test -s settings.xml
#test
test:
  override:
    - mvn mule:deploy@deploy-testch
    - echo 'Skip Tests'

  