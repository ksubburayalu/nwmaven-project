machine:
  java:
    version: openjdk8
#dependencies
dependencies:
  override:
    - mvn clean package mule:execute@deploy-devch integration-test -s settings.xml
#test
test:
  override:
    - mvn mule:execute@deploy-testch
    - echo 'Skip Tests'

  