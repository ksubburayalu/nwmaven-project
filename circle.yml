machine:
  java:
    version: openjdk8
#dependencies
dependencies:
  override:
    - mvn clean package deploy:execute@deploy-devch integration-test -s settings.xml
#test
test:
  override:
    - mvn deploy:execute@deploy-testch
    - echo 'Skip Tests'

  